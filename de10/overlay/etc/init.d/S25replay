#!/bin/sh
#
# replay        Starts replay.
#

init_replay() {
	# unzip here instead.. 
	cp /home/replay/main.elf /media/data
	cp /home/replay/replay.ini /media/data
	cp /home/replay/loader_DE.rbf /media/data
	cp /home/replay/background.raw /media/data
	cp /home/replay/loop.pcm /media/data
}

start() {
	[ ! -e /media/data/main.elf ] && init_replay
	printf "Starting replay: "
	cd /media/data && ./main.elf 2>&1 > /var/log/replay &
	touch /var/lock/replay
	echo "OK"
}
stop() {
	printf "Stopping replay: "
	killall main.elf
	rm -f /var/lock/replay
	echo "OK"
}
restart() {
	stop
	start
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?

